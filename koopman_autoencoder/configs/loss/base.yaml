# configs/loss/base.yaml
# base config for loss

# loss:
alpha: 5.0
beta: 1.0
loss_type: "l2"
grad_weight: 0.0
ssim_weight: null # 0.1
re_weight: null # 0.0
stability_weight: 0.001 # null
weighting_type: "uniform"
sigma_blur: null
