# configs/loss/base.yaml

alpha: 20.0
beta: 1.0
loss_type: "l2"
physics_weight: 1.0

gamma_time: 1.0
gamma_space: 5.0      # <--- INCREASED: (was 2.0) Force the decoder to draw sharp shock edges
gamma_spectral: 5.0

ssim_weight: null

re_weight: null        # <--- FIX THIS: You had it as 'null'. Set to 1.0 immediately.
stability_weight: 1e-5 # <--- REDUCED: (was 1e-3) The previous value was too high, dampening the dynamics.
weighting_type: "uniform" # <--- CRITICAL FIX: (was "cosine") Force the model to care about the last frame.
sigma_blur: null
