output_dir: "model_outputs"
data:
  data_dir: "./data"
  train_file: "jet/qg_jet_train_data_clean_small.nc"
  # train_file: "jet/qg_jet_train_data_clean.nc"
  val_file: "jet/qg_jet_val_data.nc"
  test_file: "jet/qg_jet_test_data.nc"
  max_sequence_length: 5  # Maximum sequence length
model:
  height: 256
  width: 256
  input_channels: 6
  hidden_dims:
    - 64
    - 128
    - 64
  block_size: 2
  kernel_size: 3
  spatial_dim: 8
  conv_kwargs:
    padding: 1
    padding_mode: "circular"
  latent_dim: 1024 # sparial_dimˆ2 * koopman latent dim
training:
  batch_size: 15
  num_epochs: 1000
  patience: 1000
  use_checkpoint: True
lr_scheduler:
  learning_rate: !!float 1e-4
  warmup_steps: 500
  decay_steps: 10000
  final_lr: !!float 1e-5
loss:
  alpha: 1.0
  beta: 0.1
